# =============================================================================
# Harpans Redovisning - Production Environment Variables
# =============================================================================
# Kopiera denna fil till .env på servern och fyll i värden

# Django Settings
SECRET_KEY=GENERERA_MED_python_-c_import_secrets_print_secrets.token_urlsafe_50
DEBUG=False
ALLOWED_HOSTS=DIN_LINODE_IP
CSRF_TRUSTED_ORIGINS=http://DIN_LINODE_IP

# Database (PostgreSQL)
DB_NAME=harpans_db
DB_USER=harpans_user
DB_PASSWORD=VÄLJ_STARKT_LÖSENORD
DB_HOST=localhost
DB_PORT=5432

# Email (Gmail exempel - kan konfigureras senare)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=info@harpans.se
EMAIL_HOST_PASSWORD=GOOGLE_APP_SPECIFIC_PASSWORD
DEFAULT_FROM_EMAIL=noreply@harpans.se

# Instagram API (lägg till senare)
INSTAGRAM_ACCESS_TOKEN=

# Wagtail
WAGTAILADMIN_BASE_URL=http://DIN_LINODE_IP

# =============================================================================
# VIKTIGT: När domän är klar, uppdatera:
# - ALLOWED_HOSTS=harpans.se,www.harpans.se
# - CSRF_TRUSTED_ORIGINS=https://harpans.se,https://www.harpans.se
# - WAGTAILADMIN_BASE_URL=https://harpans.se
# =============================================================================
